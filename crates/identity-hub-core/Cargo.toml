[package]
name = "identity-hub-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Domain types for ds-identity-hub-rs: stored credential records, DID document / service-identity construction, and a minimal in-memory Secure Token Service (STS), built on ds-dcp-core-rs's role-agnostic DCP primitives."

[dependencies]
ds-dcp-core-rs = { workspace = true }
identity-hub-graph = { path = "../identity-hub-graph" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }
regex = { workspace = true }
